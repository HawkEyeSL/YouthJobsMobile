<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/RelativeLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#8fe3ff"
    tools:context=".Main" >

    <ImageButton
        android:id="@+id/loginButton"
        android:layout_width="103dip"
        android:layout_height="50dip"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="60dip"
        android:background="@drawable/btn_login"
        android:contentDescription="Redirect ot login view"
        android:onClick="toLoginView" />

    <ImageButton
        android:id="@+id/jobsButton"
        android:layout_width="93dip"
        android:layout_height="45dip"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/loginButton"
        android:layout_marginTop="50dip"
        android:background="@drawable/btn_jobs"
        android:contentDescription="Redirect ot jobs view"
        android:onClick="toJobsView" />

    <ImageButton
        android:id="@+id/PTJobsButton"
        android:layout_width="280dip"
        android:layout_height="40dip"
        android:layout_below="@+id/jobsButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="51dip"
        android:background="@drawable/btn_part_time_jobs"
        android:contentDescription="Redirect ot part time jobs view"
        android:onClick="toPTJobsView" />

</RelativeLayout>
